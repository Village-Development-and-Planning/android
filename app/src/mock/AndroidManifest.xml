<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.puthuvaazhvu.mapping">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".application.MappingApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".views.activities.main.MainActivity" />

        <activity android:name=".views.activities.save_survey_data.SurveyDataDumpActivity" />

        <activity android:name=".views.activities.survey_list.SurveyListActivity" />

        <activity android:name=".views.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".views.activities.settings.SettingsActivity" />

        <activity android:name=".views.testing.ShownTogetherQuestionFragmentActivity" />

        <activity android:name=".views.testing.MultipleChoiceQuestionFragmentTesting" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="6fd50a18cce0b74ec2ea15fc7a97a0d21f04c10f" />

    </application>

</manifest>